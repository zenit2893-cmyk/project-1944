# Выполнение проверки рабочего каталога

Проверено 2026-09-05 в `<workspace>`, PowerShell 7.6.5. Точное время последнего аудита, текущие состояния и доказательства сохраняются в `validation-status.json`. Это запись проверки инструмента аудита и имеющихся артефактов, не игровой playtest.

| Фактически выполненная проверка | Код завершения | Наблюдавшийся результат |
| --- | --- | --- |
| `scripts/verify-cod3.ps1 -RequireThrough support-tools` | 0 | ReXGlue SDK запускается; XenosRecomp, XenonRecomp и XenonAnalyse запускаются и выводят ожидаемую справку без аргументов. |
| `scripts/verify-cod3.ps1 -RequireThrough codegen-success` | 0 | Проверены текущие XEX/manifest, непустой основной журнал, основная генерация и отдельные успешные receipts 15 mission DLL с хешами их исходников и C++-артефактами. |
| `scripts/verify-cod3.ps1 -RequireThrough native-build` | 0 | Receipt реальной сборки соответствует `cod3_pc.exe`, его хешу и AMD64 PE32+ заголовку; все 15 объявленных mission DLL существуют и имеют AMD64 PE32+ заголовки. Их хеши также записаны. |
| `scripts/verify-cod3.ps1 -SkipToolProbes -RequireThrough sdk-smoke -ReportDirectory docs/reports/validation-negative-check` | 2 | SDK-файлы и все инструменты на месте, но пропущенный запуск версии даёт `AVAILABLE`, а не `PASS`; обязательный этап корректно отклонён. Результат сохранён отдельно. |

В ходе реальной сборки аудит сначала записывал `native-build: NOT_RUN`, затем `PASS` после появления успешного receipt. В обоих случаях `boot`, `gameplay`, `render-120fps` и `physics-invariance` оставались `NOT_VERIFIED`. Так проверено, что существование настоящего скомпилированного EXE не превращается в утверждение о работающей игре.

Основная генерация на момент проверки: 94 C++-файла и 94 заголовка; 15 отдельных модулей добавляют 473 C++-файла. Эти числа служат учётом артефактов и не доказывают их семантическую корректность.

Нативная сборка, с которой сопоставлялся первый успешный аудит, завершилась в `2026-09-05T08:18:40.5824224Z`. Receipt: `analysis/cod3-pc-native-build-receipt.json`. EXE: `cod3-pc/out/build/win-amd64-relwithdebinfo/cod3_pc.exe`. Его SHA256 в этой сборке: `6019E143FC2B0D77AC75950F20F5B744C9A4D9F4DC041A21B9F23C462043CFB3`. Последующие пересборки могут изменить этот хеш; текущие значения следует брать из свежего отчёта.

Тесты аудита не запускали игру и не компилировали проект. Реальные сборки и runtime-пробы выполняются соответствующими владельцами этапов; аудит только сопоставляет их receipts и артефакты. Условия для отдельного принятия игрового запуска и 120 FPS описаны в `validation-acceptance.md`.
