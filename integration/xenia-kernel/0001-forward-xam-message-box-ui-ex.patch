diff --git a/src/kernel/xam/xam_ui.cpp b/src/kernel/xam/xam_ui.cpp
--- a/src/kernel/xam/xam_ui.cpp
+++ b/src/kernel/xam/xam_ui.cpp
@@ -550,14 +550,16 @@ u32 XamShowCommunitySessionsUI_entry(u32 r3, u32 r4) {
   return X_ERROR_FUNCTION_FAILED;
 }
 
-uint32_t XamShowMessageBoxUIEx_entry() {
-  // TODO(tomc): implement properly
-  static bool warned = false;
-  if (!warned) {
-    REXKRNL_WARN("[STUB] XamShowMessageBoxUIEx - not implemented");
-    warned = true;
-  }
-  return 0;
+u32 XamShowMessageBoxUIEx_entry(
+    u32 user_index, mapped_wstring title_ptr, mapped_wstring text_ptr,
+    u32 button_count, mapped_u32 button_ptrs, u32 active_button, u32 flags,
+    [[maybe_unused]] u32 unknown_unused, mapped_u32 result_ptr,
+    mapped_void overlapped) {
+  // Xenia 0e1307bd: Ex adds an unused eighth argument; the result and
+  // overlapped pointers are arguments 9 and 10, respectively.
+  return XamShowMessageBoxUI_entry(user_index, title_ptr, text_ptr, button_count,
+                                   button_ptrs, active_button, flags, result_ptr,
+                                   overlapped);
 }
 
 }  // namespace xam
